// PM2 ecosystem config — copy to ecosystem.config.js and fill in the values.
// DO NOT commit ecosystem.config.js (contains secrets).
// ecosystem.config.js is listed in .gitignore.

module.exports = {
  apps: [
    {
      name: 'whatsapp-clinic-bot',
      script: 'src/server.js',
      instances: 1,
      autorestart: true,
      watch: false,
      env: {
        NODE_ENV: 'production',
        PORT: 3000,

        // 360dialog — API key de wabamanagement.360dialog.io → canal → Details
        // A chave já está vinculada ao número; não é necessário phone_number_id.
        WHATSAPP_API_KEY_360D: '',

        // Número da clínica em E.164 sem '+' (ex.: 5511994295900)
        WHATSAPP_CHANNEL_NUMBER: '',

        // Token de verificação registrado no webhook do 360dialog
        WHATSAPP_WEBHOOK_VERIFY_TOKEN: '',

        // Supabase — Project Settings > API
        SUPABASE_URL: '',
        SUPABASE_SERVICE_ROLE_KEY: '',

        // Calendly booking links (Etapa 2+)
        LINK_PARTICULAR_ONLINE: '',
        LINK_PARTICULAR_PRESENCIAL: '',
        LINK_ALICE_ONLINE: '',
        LINK_ALICE_PRESENCIAL: '',
      },
    },
  ],
};
